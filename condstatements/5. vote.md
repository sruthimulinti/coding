## Eligible for vote
```c
#include<stdio.h>
main()
{
int age;
printf("enter the age");
scanf("%d",&age);
if(age>=18)
        printf("Eligible for vote");
else
        printf("not eligible for vote");
}
```
